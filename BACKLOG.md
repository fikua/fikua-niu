# Niu — Backlog

Adapter: `markdown` · Prefix: `NIU` · Brief: [PLAN.md](PLAN.md)

Ordre d'execució: **llista → autenticació → desplegament → OTEL**
(revisat 2026-08-02; l'ordre original era llista → desplegament → OTEL →
autenticació, mitigat amb Cloudflare Access com a pedaç temporal — vegeu
`PLAN.md` §8 per al raonament complet del canvi). NIU-4 va provar Google
OAuth el mateix dia i es va revertir a usuari/contrasenya perquè bloquejar
el desplegament rere una consola externa anava en contra de l'objectiu
de desplegar i iterar. Un item cada cop
(`single_item_per_session: true`).

## Items
| Key | Type | State | Title | Parent | Updated | Body |
|-----|------|-------|-------|--------|---------|------|
| NIU-1 | story | done | Llista de la compra ↔ rebost (auth stubbed) | — | 2026-08-01T15:00:00Z | Dues caixes: **A comprar** i **Rebost**. Seleccionar un ítem el mou a l'altra caixa amb animació FLIP (~250ms) i actualització optimista.<br><br>Inclou: model de dades + migracions goose, API `/api/v1/items` (GET/POST/PATCH/DELETE), escriptura a `events`, UI càlida completa (crema/verd molsa/terracota, tipografia arrodonida autoallotjada), accessibilitat (teclat, contrast AA, `aria-live`, `prefers-reduced-motion`), avatar per usuari i confeti quan la llista s'buida.<br><br>Seguretat en abast: S3 (XSS — `textContent`, CSP sense `unsafe-inline`), S7 (capçaleres), S8 (sentències preparades). Cap mutació via `GET`.<br><br>Auth **stubbed**: usuari fix, `GET /api/v1/me` el retorna. La taula `users` i `password_hash` existeixen des de la migració 1. El middleware d'auth ha de tenir ja la forma definitiva perquè NIU-4 en canviï la implementació, no l'estructura.<br><br>Detall: PLAN.md §2, §4, §7.1. `[OBERT]` a resoldre a /define: duplicats permesos o avisats (§7.1), i polling vs SSE (§2.6 — recomanació: polling ~10s + refetch al focus).<br><br>Fet quan: les dues caixes funcionen end-to-end en local, suite verda, `make test` i `make lint` passen. |
| NIU-4 | story | done | Autenticació amb usuari i contrasenya | — | 2026-08-02T12:46:00Z | Depèn de NIU-1. Agents: `fullstack-developer` + `security-engineer`. **Reordenat davant de NIU-2 — vegeu nota de canvi a `PLAN.md` §8.**<br><br>Inclou: pantalla de login amb el disseny càlid, `POST /api/v1/auth/login` i `/logout`, tokens opacs de 256 bits de `crypto/rand` amb **SHA-256 a la DB (mai el token en clar)**, cookie `HttpOnly; Secure; Path=/; SameSite=Strict`, token double-submit CSRF a totes les mutacions, bcrypt cost 12, rate limit per IP i per usuari amb backoff, comparació de temps constant, expiració i neteja de sessions, seed des d'env (`NIU_USER_A_HASH`, `NIU_USER_B_HASH`).<br><br>Substitueix `auth.StubAuthenticator` (ADR-03) per un `PasswordAuthenticator` darrere la mateixa interfície `Authenticator` — `items_handlers.go` no hauria de canviar.<br><br>Seguretat en abast: S1 (CSRF), S2 (segrest de sessió), S4 (força bruta), S5 (enumeració d'usuaris — error idèntic i temps constant), S6 (fixació de sessió), S9 (secrets fora de la imatge i del repo).<br><br>Detall: PLAN.md §3, §6, §7.2, §8. Fet quan: **tots** els tests de seguretat de §7.2 passen, i una persona amb les credencials correctes completa login → ús → logout. |
| NIU-2 | task | captured | Desplegament: Docker, compose, CI/CD, DNS, backup SQLite | — | 2026-08-02T00:00:00Z | Depèn de NIU-4 (canviat des de NIU-1 — vegeu `PLAN.md` §8). Agent: `platform-engineer`.<br><br>Inclou: Dockerfile multi-stage (distroless, non-root, binari estàtic sense CGO), `compose.yaml` a `fikua-platform-iac/projects/niu/` (font de veritat) + còpia sincronitzada a `app/compose.yaml`, tres workflows (`build.yml`, `release.yml`, `deploy.yml`) seguint el patró de `fikua-yes-very-well-fandango`, registre Docker Hub `fikua/niu`, registre A a Cloudflare via OpenTofu.<br><br>**Cloudflare Access no forma part d'aquest item.** Amb NIU-4 desplegat abans, l'app exigeix login abans de qualsevol mutació — un registre A directe cap a `websecure` és el mateix patró que ja fa servir `exam-room`, sense inventar-ne cap de nou.<br><br>⚠️ **Backup SQLite**: `scripts/backup-db.sh` només descobreix contenidors `*-db` i fa `pg_dump` → Niu se saltaria en silenci. Cal segona passada per label `fikua.backup.sqlite=/data/niu.db`, `sqlite3 .backup` (mai `cp` en calent) i **plegar la llista nova al bucle de purga**, que itera `DB_CONTAINERS`.<br><br>⚠️ Rate limit Traefik amb `sourcecriterion.requestheadername` amb el valor `Cf-Connecting-Ip` — sense això el limitador veu IPs rotatòries de Cloudflare i no dispara mai.<br><br>Els canvis a `fikua-platform-iac` són **PR separada en aquell repo** — la PR #15 existent es va escriure amb el pla d'Access i es va tancar; se'n reobre una de nova amb el DNS descomentat des del principi.<br><br>Detall: PLAN.md §5.1–5.3, §5.5, §8. Fet quan: `https://niu.fikua.com` serveix l'app, login requerit per usar-la, una release desplega, i existeix un backup **restaurat almenys un cop**. |
| NIU-3 | task | captured | Instrumentació OTEL → OpenObserve | — | 2026-08-01T00:00:00Z | Depèn de NIU-2. Agent: `platform-engineer`.<br><br>Niu serà **la primera instrumentació Go real amb SDK OpenTelemetry** de la plataforma (les existents són JVM; l'exam-room Go només fixa variables). Escriure-ho com a implementació de referència.<br><br>Inclou: SDK OTEL, middleware `otelhttp`, spans manuals a operacions de DB, atribut `user.id` a cada span.<br><br>⚠️ **Dos paranys documentats, tots dos causen pèrdua silenciosa de dades**: (1) el port és **4318** OTLP/HTTP, no 4317 — 4317 accepta la connexió i la tanca; (2) el SDK de Go va a **gRPC** per defecte, cal `OTEL_EXPORTER_OTLP_PROTOCOL: http/protobuf` explícit.<br><br>⚠️ **Canvi obligatori a l'IaC**: `platform-services/otel-collector/config.yaml` necessita exporter `otlphttp/niu`, tres pipelines i regla de match a `routing/traces`, `routing/metrics` i `routing/logs`. Sense això l'app exporta feliçment i el col·lector ho descarta tot.<br><br>Sense capçaleres d'auth a l'app — el col·lector ja les porta. El contenidor ha d'unir-se a `traefik-public` per arribar-hi.<br><br>Detall: PLAN.md §5.4, §7.2 (Observabilitat). Fet quan: una petició genera una traça completa visible a https://observability.fikua.com amb `service.name = niu`, incloent span HTTP i span de DB. |
| NIU-5 | story | planned | Compres grans i projectes de casa | — | 2026-08-03T00:00:00Z | ## Problem<br>Fora de la llista de la compra del dia a dia (NIU-1), no hi ha lloc per fer seguiment de compres més grans o projectes de casa (pintar una habitació, comprar un moble, canviar el televisor). Aquestes tenen un cicle de vida diferent: idea → decidit/pressupostat → comprat/fet, no 'a comprar ↔ rebost'.<br><br>## Customer<br>Oriol i Joana, els dos usuaris de Niu.<br><br>## Value<br>Poder centralitzar idees i projectes de casa que ara es perden en converses o notes soltes, amb visibilitat de l'estat de cadascun.<br><br>## Notes per a /define<br>- Explorar si convé una entitat nova (p.ex. `big_purchases` o `projects`) reutilitzant la taula `events` per l'historial, seguint el patró establert a PLAN.md §2.4 (mai delete+insert, UPDATE d'estat).<br>- Decidir si calen camps com pressupost estimat/real, notes, o data objectiu.<br>- Decidir si els estats són una llista simple o un flux tipus Kanban (idea → decidit → comprat/fet).<br> |
| NIU-6 | story | captured | Idees d'activitats amb preview de link | — | 2026-08-02T20:21:45Z | ## Problem<br>Sovint Oriol i Joana veuen activitats interessants (webs, Instagram, etc.) i no tenen cap lloc compartit per desar-les amb context visual abans de decidir fer-les.<br><br>## Customer<br>Oriol i Joana, els dos usuaris de Niu.<br><br>## Value<br>Una pantalla per afegir idees d'activitats enganxant un link, amb una pre-visualització (tipus targeta: títol, imatge, descripció) perquè sigui fàcil reconèixer de què tracta sense haver de tornar a obrir l'enllaç original.<br><br>## Notes per a /define<br>- Requereix link unfurling: un endpoint del backend que faci fetch server-side de la URL i parsegi Open Graph tags (og:title, og:image, og:description) — mai fetch des del navegador (problemes de CORS/CSP).<br>- Instagram sovint bloqueja l'scraping sense token d'API oficial — cal decidir com es gestiona el fallback (mostrar només el link sense preview, o un altre mecanisme).<br>- Decidir model de dades: taula nova (p.ex. `activity_ideas`) amb url, títol, imatge, descripció, afegit_per, estat (idea/planificada/feta).<br>- Considerar cache del resultat del fetch (evitar tornar a fer scraping cada vegada que es mostra la targeta).<br> |
| NIU-7 | story | captured | Suport offline (PWA): cache d'app + cua de sincronització d'accions | — | 2026-08-03T00:00:00Z | ## Problem<br>Niu ja és instal·lable com a PWA (icona niu d'ocell, `manifest.json`, meta tags iOS), però sense service worker: sense connexió l'app pot no carregar, i tocar un ítem (moure/afegir/eliminar) falla i es desfà via el rollback existent a `store.js`. Molts súpers tenen mala cobertura — exactament on Niu s'usaria més.<br><br>## Customer<br>Oriol i Joana, els dos usuaris de Niu, especialment mentre són físicament al súper.<br><br>## Value<br>Que l'app obri sempre (encara que no hi hagi xarxa) i que tocar ítems al súper sense cobertura no falli — les accions es guarden i s'apliquen soles quan torna la connexió.<br><br>## Notes per a /define<br>- Dos nivells clarament diferenciats a valorar per separat: (1) cache-first dels assets estàtics + última resposta de `GET /items` coneguda (baix risc, "app shell" clàssic); (2) cua d'accions offline (mou/afegeix/elimina) amb reintent en reconnectar (molta més complexitat).<br>- El client ja fa optimistic update amb rollback (`moveItemOptimistic` a `store.js`) — avui una acció sense xarxa ja es desfà netament amb un toast. La cua offline substituiria aquest fallback pel guardat local + reintent.<br>- Cal decidir estratègia de resolució de conflictes quan dos dispositius han fet accions offline sobre el mateix ítem abans de resincronitzar (last-write-wins per `moved_at`? basada en `events`, que ja registra l'historial sense delete+insert?).<br>- Items creats offline no tenen ID de servidor fins a sincronitzar — cal ID temporal local + reconciliació, amb cura de no duplicar si l'usuari afegeix el mateix nom des de dos dispositius offline alhora.<br>- UX pendent de definir: com es marca un ítem "pendent de sincronitzar" a la UI, i què veu l'usuari si una sincronització falla definitivament (conflicte no resoluble automàticament).<br>- Decidir si val la pena separar en dos items (`/define` un per cada nivell) donada la diferència de risc i esforç entre tots dos.<br> |
